services:
  docs:
    image: ghcr.io/TheNextLvl-net/docs:latest
    restart: unless-stopped
    ports:
      - "80:3000"
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:3000"]
      interval: 30s
      timeout: 5s
      retries: 3